i18n.merge_file(
  input: 'org.xfce.PanelProfiles.appdata.xml.in',
  output: 'org.xfce.PanelProfiles.appdata.xml',
  install: true,
  install_dir: get_option('prefix') / get_option('datadir') / 'metainfo',
  po_dir: '..' / '..' / 'po',
)
